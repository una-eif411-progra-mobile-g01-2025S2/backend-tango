spring.datasource.url=jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=true
spring.flyway.enabled=false
spring.profiles.active=initlocal

# JWT Configuration
app.jwt.secret=VGVzdFNlY3JldEtleUZvclBBSS0xMjM0NTY3ODkwMTIzNDU2Nzg5MDE=
app.jwt.issuer=PAI Backend Test
app.jwt.access-token-ttl=PT15M
app.jwt.refresh-token-ttl=P7D

# OpenRouter API Configuration (for tests)
openrouter.api.key=test-key
openrouter.api.url=https://openrouter.ai/api/v1/chat/completions
openrouter.model=tngtech/deepseek-r1t2-chimera:free

# Server Configuration
server.port=8080
server.address=0.0.0.0

# CORS Configuration
cors.allowed-origins=http://localhost:3000
spring.web.cors.allowed-origins=${cors.allowed-origins}

# URL Configuration
url.user.signup=/api/v1/users/signup
app.base-url=http://localhost:8080

# API Configuration
api.base-path=/api/v1
api.endpoints.ping=${api.base-path}/ping
api.endpoints.users=${api.base-path}/users

